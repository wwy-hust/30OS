#include "stdio.h"
#include "string.h"

#include "graphic.h"
#include "app_funcUtils.h"
#include "icon.c"
#include "tim.h"
#include "wnd.h"

uint8 buf[32];
uint8 buf1[128];
void func_main()
{
	FILE_HANDLE* file, *file1;
	FILE_HANDLE fileStruct;
	int i, j;
	
	api_puts("Read Files..\n");
	file = api_open("COLOR.BIN");
	api_memcpy(file, sizeof(FILE_HANDLE), &fileStruct);
	sprintf(buf, "*%d %d %d %d* %d\n", fileStruct, fileStruct.buf, fileStruct.size, &fileStruct.pos, api_file_size(file));
	api_puts(buf);

//	file = file1;
	j = api_read(file, buf, 32);
	sprintf(buf1, "read:%d\n", j);
	api_puts(buf1);
	sprintf(buf1, "%d %d %d %d %d %d %d %d %d %d\n", buf[0], buf[1], buf[2], buf[3], buf[4], buf[5], buf[6], buf[7], buf[8], buf[9]);
	api_puts(buf1);
	api_end();
}
